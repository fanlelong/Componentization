<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ancely.fyw.eventbus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/eventbus_ic_launcher"
        android:name=".debug.EventApplication"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/eventbus_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".debug.DebugEventbusActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".debug.Event2Activity">
        </activity>
    </application>

</manifest>