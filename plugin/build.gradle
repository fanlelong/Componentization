apply plugin: 'com.android.application'
apply from: "${rootDir}/comm.gradle"
android {
    defaultConfig {
        applicationId "com.plugin.text"
    }
}

dependencies {
    implementation project(':common')
    implementation project(':network')
}